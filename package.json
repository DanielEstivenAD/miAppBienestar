{
  "name": "api_mibienestarsena_711_v1",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "start": "npm run migrate && node server.js",
    "dev": "nodemon server.js",

    "migrate": "npx sequelize-cli db:migrate --env test",
    "migrate:undo": "npx sequelize-cli db:migrate:undo:all --env test",

    "seed": "npx sequelize-cli db:seed:all --env test",
    "seed:undo": "npx sequelize-cli db:seed:undo:all --env test",
    
    "reset-db": "npx sequelize-cli db:drop --env test && npx sequelize-cli db:create --env test && npx sequelize-cli db:migrate --env test"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "express": "^5.1.0",
    "mysql2": "^3.14.3",
    "pg": "^8.16.3",
    "sequelize": "^6.37.7",
    "sequelize-cli": "^6.6.3"
  },
  "devDependencies": {
    "dotenv": "^17.2.1",
    "nodemon": "^3.1.10"
  }
}